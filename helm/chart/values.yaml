sistemahidrico:
  controllers:

    backend:
      type: deployment
      strategy: RollingUpdate
      rollingUpdate:
        unavailable: 1
      containers:
        main:
          securityContext:
            runAsUser: 1001
            runAsGroup: 1001
          image:
            pullPolicy: Always
            repository: ghcr.io/cero97-ctrl/sistemahidrico-backend
            tag: latest
          env:
            AIS_API_KEY:
              secretKeyRef:
                name: sistemahidrico-env
                key: AIS_API_KEY
            OPENSKY_CLIENT_ID:
              secretKeyRef:
                name: sistemahidrico-env
                key: OPENSKY_CLIENT_ID
            OPENSKY_CLIENT_SECRET:
              secretKeyRef:
                name: sistemahidrico-env
                key: OPENSKY_CLIENT_SECRET

    frontend:
      type: deployment
      strategy: RollingUpdate
      rollingUpdate:
        unavailable: 1
      containers:
        main:
          securityContext:
            runAsUser: 1001
            runAsGroup: 1001
          image:
            pullPolicy: Always
            repository: ghcr.io/cero97-ctrl/sistemahidrico-frontend
            tag: latest

          env:
            BACKEND_URL: 'http://sistemahidrico-backend.sistemahidrico.svc.cluster.local:8000'

  service:
    backend:
      controller: backend
      type: ClusterIP
      ports:
        http:
          port: 8000
    frontend:
      controller: frontend
      type: ClusterIP
      ports:
        http:
          port: 3000
